import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  colors: {
    primary: {
      100: "#E3F2FD",
      200: "#90CAF9",
      500: "#2196F3",
      700: "#1976D2",
    },
  },
});

export default theme;
